@inject IAuthenticationSchemeProvider SchemeProvider

@using Homefind.Infrastructure.Identity
@using Microsoft.AspNetCore.Authentication
@using Microsoft.AspNetCore.Identity
@model Homefind.Web.Models.AccountViewModels.LoginViewModel
@inject SignInManager<ApplicationUser> SignInManager
<!-- Page Banner Start -->
<div id="page-banner-area" class="page-banner">
    <div class="page-banner-title">
        <div class="text-center">
            <h2>Log</h2>
            <a href="#"><i class="lni-home"></i> Home</a>
            <span class="crumbs-spacer"><i class="lni-chevron-right"></i></span>
            <span class="current">Log</span>
        </div>
    </div>
</div>
<!-- Page Banner End -->
<!-- Content section Start -->
<section id="content" class="section-padding">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-5 col-md-6 col-xs-12">
                <div class="page-login-form box">
                    <h3>
                        Login
                    </h3>
                    <form class="login-form" asp-action="Login" asp-controller="Account">
                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text"><i class="lni-envelope"></i></span>
                            </div>
                            <input type="email" asp-for="Email" class="form-control" placeholder="Email" required>
                        </div>
                        <div class="input-group mb-3">
                            <div class="input-group-prepend">
                                <span class="input-group-text"><i class="lni-unlock"></i></span>
                            </div>
                            <input type="password" asp-for="Password" class="form-control" placeholder="Password" required>
                        </div>
                        <button type="submit" class="btn btn-common log-btn">Sign In</button>
                        @*<button type="submit" class="btn btn-danger log-btn" style="background-color: rgb(217,48,37)">
                            <i class="fab fa-google-plus-square"></i> Sign in with Google
                        </button>
                        <button type="submit" class="btn btn-primary log-btn" style="background-color: rgb(66,103,178)">
                            <i class="fab fa-facebook"></i> Sign in with Facebook
                        </button>*@
                    </form>
                    <ul class="form-links">
                        <li class="text-center"><a href='@Url.Action("Register", "Account")'>Don't have an account?</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- Content section End -->

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
